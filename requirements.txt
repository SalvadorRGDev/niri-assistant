pyopen-wakeword
onnxruntime
sounddevice
numpy
scipy
faster-whisper
ollama
pydantic
piper-tts       # TTS local por defecto; requiere una voz ES en models/piper/, ver README.md
edge-tts        # respaldo opcional solo para frases públicas; nube desactivada por defecto
jeepney         # cliente D-Bus puro en Python (control de música vía MPRIS), sin dependencias de sistema
tokenizers      # tokenizador del codificador de embeddings (src/embeddings.py); ya lo arrastra faster-whisper, se declara porque se importa directo
